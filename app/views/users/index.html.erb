<div class="container">
    <h1>Utilisateurs</h1>
    <div class="row">
        <table class="table">
        <tr id="tabBar" class="">
            <th class="">Nom</th>
            <th class="">Mot de passe</th>
            <th class="">Détails</th>
             <th class="">Supprimer</th>
        </tr>
        
        <% @users.each do |user| %>
            <tr>
            <td><%= user.nom %></td>
            <td><%= user.mot %></td>
            <td><%= link_to 'Show', user_path(user) %></td>
             <td><%= link_to 'x', user_path(user), method:'delete' %></td>
            </tr>
        <% end %>
        </table>
    </div>
</div>